[project]
name = "AutoMouseMover"
version = "0.1.0"
description = "Nudge the macOS mouse cursor periodically to prevent screen sleep"
requires-python = ">=3.10"

[tool.pyright]
include = ["app", "tests"]
pythonVersion = "3.10"
